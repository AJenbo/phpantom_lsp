{{-- The template a renderEach() falls back to when the collection it
     iterates is empty. It is handed nothing at all, so it declares no
     signature and reads no variables. --}}
<tr>
    <td colspan="3">{{ __('messages.welcome') }}</td>
</tr>
